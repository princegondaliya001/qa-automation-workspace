# 2026-05-12

## Waydroid mobile view repair / ChromaStudio mobile T2I
- Prince asked to backtrack and resolve why the noVNC mobile view was showing a desktop-style browser/window instead of proper Waydroid mobile sizing.
- Screenshot showed noVNC at `127.0.0.1:6080/vnc.html` displaying a tall narrow desktop/browser-like window with ChromaStudio result placeholder, not a true Android/Waydroid mobile viewport.
- Important standing constraints remain:
  - Desktop daily cron must stay desktop-only; do not mix in Waydroid/mobile.
  - noVNC/VNC must remain local-only through SSH tunnel.
  - Do not click Generate unless Internal Mode is verified ON.
- I began fixing the mobile view path by editing `/root/.openclaw/workspace/state/start-tigervnc-bridge.sh` so the noVNC bridge crops/serves the Waydroid/Weston phone window instead of a stale Selenium/desktop Chromium region.
- New bridge behavior intended:
  - Stop old `x0vncserver` and `websockify` processes for ports `5901`/`6080` only.
  - Use `xdotool`/`xwininfo` to find the visible `Weston Compositor` X11 window, raise it, detect geometry, and set TigerVNC `-Geometry` dynamically.
  - Serve `x0vncserver` on `127.0.0.1:5901` and `websockify` on `127.0.0.1:6080`.
  - Log crop geometry to `/root/.openclaw/workspace/state/tigervnc-geometry.log`.
- Need next continuation after compaction:
  1. Spawn focused sub-agent for Waydroid mobile-view repair if tooling supports it.
  2. Run `/root/.openclaw/workspace/state/start-tigervnc-bridge.sh` and verify listeners/geometry.
  3. If Weston window lookup fails, inspect X display/window list and adjust name/class matching.
  4. Verify Prince can connect via SSH tunnel to `http://127.0.0.1:6080/vnc.html?host=127.0.0.1&port=6080&path=websockify&autoconnect=true&resize=scale`.
  5. Only after visual mobile viewport is correct, continue with ChromaStudio mobile T2I Waydroid smoke.

## Waydroid mobile visual repair
- Prince reported noVNC mobile view showed a desktop Chromium-style window instead of a proper Waydroid mobile viewport.
- Root cause: TigerVNC bridge was cropping `414x768+433+32`, which captured the center of the Xvfb desktop/desktop browser rather than the Weston/Waydroid phone window at `420x900+0+0`; stale Selenium Chromium windows also covered the viewport.
- Fixed `/root/.openclaw/workspace/state/start-tigervnc-bridge.sh` to locate/raise the `Weston Compositor` X11 window and crop noVNC to its geometry, local-only on `127.0.0.1:6080` -> `127.0.0.1:5901`.
- Verified final crop as real Android mobile UI with status/navigation bars: `/root/.openclaw/workspace/state/waydroid-debug/novnc-final-mobile-crop.png`.
- Patched and committed Maestro mobile smoke wrapper to include `/root/.maestro/bin` in PATH and use `maestro test --udid`: commit `abafc99 Fix Waydroid mobile smoke PATH`.
- Maestro mobile run is still blocked after visual fix: `io.grpc.StatusRuntimeException: UNAVAILABLE`, `Command failed (tcp:7001): closed`; debug log `/root/.maestro/tests/2026-05-12_035538/maestro.log`. ADB itself shows `192.168.240.112:5555 device` and `sys.boot_completed=1`.

## Waydroid mobile-only Bria 3.2 generation success
- Prince clarified he wanted only Waydroid/mobile-view testing, not desktop view.
- Desktop Maestro generation rotation was stopped because it kept spawning Selenium Chromium/WebDriver into the desktop display; mobile visual remains isolated on `Xvfb :98` with noVNC local-only at `127.0.0.1:6080 -> 127.0.0.1:5901`.
- Mobile smoke passed on Waydroid device `192.168.240.112:5555`, but Maestro/ADB and manual taps were unreliable for actual prompt input/generate.
- Reliable solution used Android WebView DevTools/CDP from Jelly:
  - `adb forward tcp:9223 localabstract:webview_devtools_remote_2392`
  - Set Bria route `https://chromastudio.ai/text-to-image?type=bria-3-2-t2i`
  - Set prompt via DOM input events.
  - Enabled Internal Mode by real CDP mouse events on the visible switch.
  - Clicked Generate by real CDP mouse event.
- Successful evidence directory:
  - `/root/.openclaw/workspace/state/waydroid-debug/cdp-dom-real-click-20260512-042627/`
  - `screen-0.png` shows `Generating... Please wait`.
  - `screen-1.png` and `screen-6.png` show completed generated result.
  - `summary.txt` records prompt/Internal Mode/generation state.
- New result thumbnail URL observed after completion:
  - `https://assets.chromastudio.ai/textToImage/cyr8pp3YnOdX0htHr7Px8rfXRan2/e78214ba-e877-4904-8d9f-c529c161f323/thumbnail_0_1778560003241.webp`

## Cross-surface Maestro testing cron agent implemented
- Prince requested one managed cron/testing agent that runs in order: ChromaStudio desktop view test, MaxStudio desktop video test, Waydroid ChromaStudio mobile test, then Waydroid MaxStudio mobile check, with Discord alerts on any flow/generation failure.
- Implemented in Maestro repo commit `fd7ecc1 Add cross-surface scheduled testing agent`.
- New files:
  - `/root/.openclaw/workspace/repos/maestro-studio/scripts/cross-surface-testing-agent.sh`
  - `/root/.openclaw/workspace/repos/maestro-studio/scripts/cross_surface_testing_agent.py`
  - `/root/.openclaw/workspace/repos/maestro-studio/scripts/waydroid_chromastudio_bria_t2i_cdp.py`
- Registered OpenClaw cron job:
  - id `8c19bf94-68e4-45d2-b976-8152e16b1065`
  - name `cross-surface-maestro-testing-agent`
  - schedule `15 10 * * *` Asia/Calcutta
- Agent behavior:
  - Desktop stages use `DISPLAY=:99`.
  - Waydroid/mobile stages kill desktop WebDriver before running and use `192.168.240.112:5555`, isolated noVNC/mobile setup remains `:98`.
  - Uses `/root/.openclaw/workspace/state/maestro-discord.env` for Discord alerts without printing webhook.
  - Continues after individual failures and sends redacted Discord failure + summary messages.
- Validation run performed:
  - Python compile for new scripts passed.
  - Dry-run of ordered stages passed.
  - `maestro check-syntax maxstudio/schema/desktop/text-to-video/wan-v2-6-t2v-create.yaml` passed.
